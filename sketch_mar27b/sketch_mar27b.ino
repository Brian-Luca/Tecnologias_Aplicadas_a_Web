// Controlar um LED através da saída digital

int LED = 13; // Associa o pino 13 a variavel LED
int LEDq = 12;
int LEDw = 11;
int LEDe = 10;

void setup() {
   pinMode(LED, OUTPUT); // Define o pino 13 como saída 
   pinMode(LEDq, OUTPUT);
   pinMode(LEDw, OUTPUT);
   pinMode(LEDe, OUTPUT);
} 

void loop() { 
   digitalWrite(LED, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(100); // Aguarda 1 segundo
   digitalWrite(LED, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(100); // Aguarda 1 segundo
   digitalWrite(LEDq, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(100); // Aguarda 1 segundo
   digitalWrite(LEDq, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(100); // Aguarda 1 segundo
   digitalWrite(LEDw, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(100); // Aguarda 1 segundo
   digitalWrite(LEDw, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(100); // Aguarda 1 segundo
   digitalWrite(LEDw, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(100); // Aguarda 1 segundo
   digitalWrite(LEDw, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(100); // Aguarda 1 segundo
   digitalWrite(LEDw, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(100); // Aguarda 1 segundo
   digitalWrite(LEDw, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(100); // Aguarda 1 segundo
   digitalWrite(LEDw, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(100); // Aguarda 1 segundo
   digitalWrite(LEDw, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(100); // Aguarda 1 segundo
   digitalWrite(LEDw, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(100); // Aguarda 1 segundo
   digitalWrite(LEDw, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(100); // Aguarda 1 segundo
   digitalWrite(LEDe, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(100); // Aguarda 1 segundo
   digitalWrite(LEDe, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(100); // Aguarda 1 segundo
   digitalWrite(LED, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(50); //Aguarda 0,5 segundo
   digitalWrite(LED, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(50); // Aguarda 0,5 segundo
   digitalWrite(LEDq, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(50); // Aguarda 0,5 segundo
   digitalWrite(LEDq, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(50);  
   digitalWrite(LEDw, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(50); // Aguarda 0,5 segundo
   digitalWrite(LEDw, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(50);  // Aguarda 0,5 segundo
   digitalWrite(LEDe, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(50); // Aguarda 0,5 segundo
   digitalWrite(LEDe, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(50);
   digitalWrite(LEDe, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(50); // Aguarda 0,5 segundo
   digitalWrite(LEDe, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(50);
   digitalWrite(LEDe, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(50); // Aguarda 0,5 segundo
   digitalWrite(LEDe, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(50);
   digitalWrite(LEDe, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(50); // Aguarda 0,5 segundo
   digitalWrite(LEDe, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(50);
   digitalWrite(LEDe, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(50); // Aguarda 0,5 segundo
   digitalWrite(LEDe, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(50);
   digitalWrite(LEDe, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(50); // Aguarda 0,5 segundo
   digitalWrite(LEDe, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(50);
   digitalWrite(LED, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(250); //Aguarda 0,5 segundo
   digitalWrite(LED, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(250); // Aguarda 0,5 segundo
   digitalWrite(LEDq, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(250); // Aguarda 0,5 segundo
   digitalWrite(LEDq, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(250);
   digitalWrite(LEDq, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(250); // Aguarda 0,5 segundo
   digitalWrite(LEDq, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(250);
   digitalWrite(LEDq, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(250); // Aguarda 0,5 segundo
   digitalWrite(LEDq, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(250);
   digitalWrite(LEDq, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(250); // Aguarda 0,5 segundo
   digitalWrite(LEDq, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(250);
   digitalWrite(LEDq, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(250); // Aguarda 0,5 segundo
   digitalWrite(LEDq, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(250);  
   digitalWrite(LEDw, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(250); // Aguarda 0,5 segundo
   digitalWrite(LEDw, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(250);  // Aguarda 0,5 segundo
   digitalWrite(LEDe, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(250); // Aguarda 0,5 segundo
   digitalWrite(LEDe, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(250); 
   digitalWrite(LED, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(50); //Aguarda 0,5 segundo
   digitalWrite(LED, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(50); // Aguarda 0,5 segundo
    digitalWrite(LED, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(50); //Aguarda 0,5 segundo
   digitalWrite(LED, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(50); // Aguarda 0,5 segundo
    digitalWrite(LED, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(50); //Aguarda 0,5 segundo
   digitalWrite(LED, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(50); // Aguarda 0,5 segundo
   digitalWrite(LEDq, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(50); // Aguarda 0,5 segundo
   digitalWrite(LEDq, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(50);  
   digitalWrite(LEDw, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(50); // Aguarda 0,5 segundo
   digitalWrite(LEDw, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(50);  // Aguarda 0,5 segundo
   digitalWrite(LEDe, HIGH); // Põe nível 1 no pino 13 (acende o LED) 
   delay(50); // Aguarda 0,5 segundo
   digitalWrite(LEDe, LOW); // Põe nível 0 no pino 13 (apaga o LED) 
   delay(50);     
}
